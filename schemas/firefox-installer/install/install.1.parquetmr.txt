message install {
  required group metadata {
    required binary documentId (UTF8);
    required int64 Timestamp;
    required binary geoCountry (UTF8);
    required binary geoCity (UTF8);
  }
  required binary installer_type (UTF8);
  required binary installer_version (UTF8);
  required binary build_channel (UTF8);
  optional binary update_channel (UTF8);
  optional binary version (UTF8);
  optional binary build_id (UTF8);
  optional binary locale (UTF8);
  optional boolean from_msi;
  optional boolean 64bit_build;
  optional boolean 64bit_os;
  optional binary os_version (UTF8);
  optional binary service_pack (UTF8);
  optional boolean server_os;
  optional boolean admin_user;
  optional boolean default_path;
  optional boolean set_default;
  optional boolean new_default;
  optional boolean old_default;
  optional boolean had_old_install;
  optional binary old_version (UTF8);
  optional binary old_build_id (UTF8);
  optional int64 bytes_downloaded;
  optional int64 download_size;
  optional int64 download_retries;
  optional int64 download_time;
  optional int64 download_latency;
  optional binary download_ip (UTF8);
  optional boolean manual_download;
  optional int64 intro_time;
  optional int64 options_time;
  optional int64 preinstall_time;
  optional int64 install_time;
  optional int64 finish_time;
  optional boolean succeeded;
  optional boolean disk_space_error;
  optional boolean no_write_access;
  optional boolean user_cancelled;
  optional boolean out_of_retries;
  optional boolean file_error;
  optional boolean sig_not_trusted;
  optional boolean sig_unexpected;
  optional boolean install_timeout;
  optional boolean new_launched;
  optional boolean old_running;
  optional binary attribution (UTF8);
  optional binary profile_cleanup_prompt (UTF8);
  optional boolean profile_cleanup_requested;
}
