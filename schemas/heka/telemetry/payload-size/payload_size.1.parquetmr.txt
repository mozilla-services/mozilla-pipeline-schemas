message telemetry_payload_budget {
  required binary build (UTF8);
  required binary channel (UTF8);
  required binary docType (UTF8);
  required int64 size;
  required binary submissionDate (UTF8);
}
