{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "properties": {
    "changesetID": {
      "description": "The 40 char hex ID of the changeset. See https://www.mercurial-scm.org/wiki/ChangeSetID",
      "pattern": "[a-f0-9]{40}",
      "type": "string"
    },
    "pushDate": {
      "description": "Integer UNIX timestamp of when the push to the repository occurred, as recorded by the repository pushlog",
      "type": "integer"
    },
    "repository": {
      "description": "The URL of the repository the changeset was pushed to",
      "type": "string"
    },
    "reviewSystemUsed": {
      "description": "Which code review system was used to review this changeset?",
      "enum": [
        "bmo",
        "mozreview",
        "phabricator",
        "unknown",
        "not_applicable"
      ],
      "type": "string"
    }
  },
  "required": [
    "changesetID",
    "reviewSystemUsed",
    "repository"
  ],
  "title": "hgpush",
  "type": "object"
}
