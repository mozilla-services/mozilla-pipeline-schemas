bigquery:
  clustering:
    fields:
    - normalized_channel
    - sample_id
  time_partitioning:
    field: submission_date
    require_partition_filter: true
    type: day
mozPipelineMetadata:
  bq_dataset_family: org_mozilla_firefox_beta
  bq_metadata_format: structured
  bq_table: first_session_v1
source_schema: org-mozilla-firefox-beta/first-session/first-session.1.schema.json
