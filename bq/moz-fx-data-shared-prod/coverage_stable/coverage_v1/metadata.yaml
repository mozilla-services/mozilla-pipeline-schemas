bigquery:
  clustering:
    fields:
    - normalized_channel
    - sample_id
  time_partitioning:
    field: submission_date
    require_partition_filter: true
    type: day
mozPipelineMetadata:
  bq_dataset_family: coverage
  bq_metadata_format: structured
  bq_table: coverage_v1
source_schema: coverage/coverage/coverage.1.schema.json
