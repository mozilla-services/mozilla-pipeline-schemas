bigquery:
  clustering:
    fields:
    - normalized_channel
    - sample_id
  time_partitioning:
    field: submission_date
    require_partition_filter: true
    type: day
mozPipelineMetadata:
  bq_dataset_family: mozdata
  bq_metadata_format: structured
  bq_table: event_v1
source_schema: mozdata/event/event.1.schema.json
