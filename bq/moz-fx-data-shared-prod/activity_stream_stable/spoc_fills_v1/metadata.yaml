bigquery:
  clustering:
    fields:
    - normalized_channel
    - sample_id
  time_partitioning:
    field: submission_date
    require_partition_filter: true
    type: day
mozPipelineMetadata:
  bq_dataset_family: activity_stream
  bq_metadata_format: structured
  bq_table: spoc_fills_v1
  expiration_policy:
    delete_after_days: 180
source_schema: activity-stream/spoc-fills/spoc-fills.1.schema.json
