bigquery:
  clustering:
    fields:
    - normalized_channel
    - sample_id
  time_partitioning:
    field: submission_date
    require_partition_filter: true
    type: day
mozPipelineMetadata:
  bq_dataset_family: org_mozilla_ios_klar
  bq_metadata_format: structured
  bq_table: baseline_v1
  expiration_policy:
    delete_after_days: 720
source_schema: org-mozilla-ios-klar/baseline/baseline.1.schema.json
