{
  "description": "Optional, excluded if there was no error.",
  "type": "object",
  "properties": {
    "name": {
      "description": "httperror, networkerror, shutdownerror, etc.",
      "type": "string"
    },
    "code": {
      "description": "Only present for httperror and networkerror.",
      "type": "integer"
      
    },
    "error": {
      "description": "Only present for othererror and unexpectederror.",
      "type": "string"
    },
    "from": {
      "description": "Optional, and only present for autherror.",
      "type": "string"
    }
  }
}