message downgrade {
  required binary id (UTF8);
  required binary creationDate (UTF8);
  @TELEMETRY_APPLICATION_1_TXT@
  required binary clientId (UTF8);
  required double version;
  required group payload {
    required binary lastVersion (UTF8);
    required binary lastBuildId (UTF8);
    required boolean hasSync;
    required boolean hasBinary;
    required int64 button;
  }
}
