message telemetry_payload_size {
  required int64 Timestamp;
  required group Fields {
    required binary build (UTF8);
    required binary channel (UTF8);
    required binary docType (UTF8);
    required int64 size;
  }
}
